import { TransTypePipe } from './trans-type.pipe';

describe('TransTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TransTypePipe();
    expect(pipe).toBeTruthy();
  });
});
